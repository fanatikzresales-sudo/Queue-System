{
  "name": "queue-drop-timer",
  "version": "1.0.0",
  "description": "Walmart Wednesday queue simulator + step-down refresh delay planner that lands the final refresh exactly at queue go-live.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
